type Query {
  banners: [Banner]
}

type Mutation {
  addBanner(data: addBannerInput!): Banner @auth(requires: ADMIN)
}

input addBannerInput {
  title: String!
  image: String!
}

type Banner {
  id: ID
  title: String
  image: String
  publicId: String
  createdAt: Date
  updatedAt: Date
}
