type Mutation {
  login(username: String!, password: String!): loginData
}

type loginData {
  message: String
  user: loginUserData
  token: String
}

type loginUserData {
  fullName: String
  username: String
  role: String
}
